<% title "Edit a Kitten" %>
<% provide(:button_text, "Save that cat!") %>

<h1>Edit a Kitten</h1>

<%= render 'shared/form' %>
<%= link_to "Delete Kitten", kitten_path(@kitten), method: :delete, data: { confirm: "Delete! Really?" } %>